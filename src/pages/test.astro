---
import Layout from '../layouts/Layout.astro';
import { supabase } from '../lib/supabase';

// Test database connection by fetching categories
const { data: categories, error } = await supabase
  .from('categories')
  .select('*')
  .limit(1);
---

<Layout title="Test Connection">
  <div class="p-8">
    <h1 class="text-2xl font-bold mb-4">Supabase Connection Test</h1>
    
    {error ? (
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
        <p>Error: {error.message}</p>
        <p class="mt-2">SUPABASE_URL: {import.meta.env.SUPABASE_URL ? 'Set' : 'Not Set'}</p>
        <p>SUPABASE_ANON_KEY: {import.meta.env.SUPABASE_ANON_KEY ? 'Set' : 'Not Set'}</p>
      </div>
    ) : (
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded">
        <p>Successfully connected to Supabase!</p>
        <p class="mt-2">Found {categories?.length} categories</p>
      </div>
    )}
  </div>
</Layout>
</boltArtifact>

Let's start the development server:

<boltArtifact id="start-dev-server" title="Start development server">
<boltAction type="start">
<command>npm run dev</command>